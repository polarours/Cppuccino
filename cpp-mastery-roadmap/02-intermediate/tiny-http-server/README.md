# tiny-http-server

A minimal HTTP server written in modern C++.

This project is part of the **Cppuccino** learning roadmap.

## Features

- TCP socket server
- HTTP request parsing
- Simple router
- HTTP response builder

## Build
```bash
mkdir build
cd build
cmake ..
make
```

## Usage
```bash
./tiny-http-server
```